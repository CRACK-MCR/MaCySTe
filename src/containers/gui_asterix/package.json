{
  "name": "gui-asterix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "npx esbuild src/index.js --bundle --sourcemap --target=es2020 --minify --external:crypto --outfile=app/index.js --format=iife",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "nats.ws": "^1.11.2"
  },
  "devDependencies": {
    "esbuild": "^0.16.12"
  }
}
